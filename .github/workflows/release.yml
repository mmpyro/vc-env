name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build binaries
        run: |
          mkdir -p build
          GOOS=darwin GOARCH=arm64 go build -o build/vc-env-darwin-arm64 ./cmd/vc-env
          GOOS=darwin GOARCH=amd64 go build -o build/vc-env-darwin-amd64 ./cmd/vc-env
          GOOS=linux GOARCH=arm64 go build -o build/vc-env-linux-arm64 ./cmd/vc-env
          GOOS=linux GOARCH=amd64 go build -o build/vc-env-linux-amd64 ./cmd/vc-env

      - name: Generate release notes
        id: release_notes
        run: |
          CURRENT_TAG="${{ github.ref_name }}"
          PREVIOUS_TAG=$(git tag --sort=-version:refname | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | sed -n '2p')

          if [ -n "$PREVIOUS_TAG" ]; then
            NOTES=$(git log "${PREVIOUS_TAG}..${CURRENT_TAG}" --pretty=format:"- %s (%h)" --no-merges)
          else
            NOTES=$(git log "${CURRENT_TAG}" --pretty=format:"- %s (%h)" --no-merges)
          fi

          if [ -z "$NOTES" ]; then
            NOTES="No changes recorded."
          fi

          {
            echo "notes<<EOF"
            echo "$NOTES"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          body: ${{ steps.release_notes.outputs.notes }}
          files: |
            build/vc-env-darwin-arm64
            build/vc-env-darwin-amd64
            build/vc-env-linux-arm64
            build/vc-env-linux-amd64

      - name: Upload binaries as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: vc-env-binaries-${{ github.ref_name }}
          path: |
            build/vc-env-darwin-arm64
            build/vc-env-darwin-amd64
            build/vc-env-linux-arm64
            build/vc-env-linux-amd64
